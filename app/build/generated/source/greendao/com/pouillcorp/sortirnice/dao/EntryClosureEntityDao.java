package com.pouillcorp.sortirnice.dao;

import java.util.List;
import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;
import org.greenrobot.greendao.query.Query;
import org.greenrobot.greendao.query.QueryBuilder;

import com.pouillcorp.sortirnice.entities.entry.join.JoinEntryEntityWithEntryClosureEntity;

import com.pouillcorp.sortirnice.entities.entry.detail.EntryClosureEntity;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "ENTRY_CLOSURE_ENTITY".
*/
public class EntryClosureEntityDao extends AbstractDao<EntryClosureEntity, Long> {

    public static final String TABLENAME = "ENTRY_CLOSURE_ENTITY";

    /**
     * Properties of entity EntryClosureEntity.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property Id = new Property(0, Long.class, "id", true, "_id");
        public final static Property ClosureDay = new Property(1, String.class, "closureDay", false, "CLOSURE_DAY");
        public final static Property ClosureSpan = new Property(2, String.class, "closureSpan", false, "CLOSURE_SPAN");
        public final static Property Date = new Property(3, String.class, "date", false, "DATE");
    }

    private Query<EntryClosureEntity> entryEntity_ListClosuresQuery;

    public EntryClosureEntityDao(DaoConfig config) {
        super(config);
    }
    
    public EntryClosureEntityDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"ENTRY_CLOSURE_ENTITY\" (" + //
                "\"_id\" INTEGER PRIMARY KEY ," + // 0: id
                "\"CLOSURE_DAY\" TEXT," + // 1: closureDay
                "\"CLOSURE_SPAN\" TEXT," + // 2: closureSpan
                "\"DATE\" TEXT);"); // 3: date
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"ENTRY_CLOSURE_ENTITY\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, EntryClosureEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String closureDay = entity.getClosureDay();
        if (closureDay != null) {
            stmt.bindString(2, closureDay);
        }
 
        String closureSpan = entity.getClosureSpan();
        if (closureSpan != null) {
            stmt.bindString(3, closureSpan);
        }
 
        String date = entity.getDate();
        if (date != null) {
            stmt.bindString(4, date);
        }
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, EntryClosureEntity entity) {
        stmt.clearBindings();
 
        Long id = entity.getId();
        if (id != null) {
            stmt.bindLong(1, id);
        }
 
        String closureDay = entity.getClosureDay();
        if (closureDay != null) {
            stmt.bindString(2, closureDay);
        }
 
        String closureSpan = entity.getClosureSpan();
        if (closureSpan != null) {
            stmt.bindString(3, closureSpan);
        }
 
        String date = entity.getDate();
        if (date != null) {
            stmt.bindString(4, date);
        }
    }

    @Override
    public Long readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0);
    }    

    @Override
    public EntryClosureEntity readEntity(Cursor cursor, int offset) {
        EntryClosureEntity entity = new EntryClosureEntity( //
            cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0), // id
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // closureDay
            cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2), // closureSpan
            cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3) // date
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, EntryClosureEntity entity, int offset) {
        entity.setId(cursor.isNull(offset + 0) ? null : cursor.getLong(offset + 0));
        entity.setClosureDay(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setClosureSpan(cursor.isNull(offset + 2) ? null : cursor.getString(offset + 2));
        entity.setDate(cursor.isNull(offset + 3) ? null : cursor.getString(offset + 3));
     }
    
    @Override
    protected final Long updateKeyAfterInsert(EntryClosureEntity entity, long rowId) {
        entity.setId(rowId);
        return rowId;
    }
    
    @Override
    public Long getKey(EntryClosureEntity entity) {
        if(entity != null) {
            return entity.getId();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(EntryClosureEntity entity) {
        return entity.getId() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
    /** Internal query to resolve the "listClosures" to-many relationship of EntryEntity. */
    public List<EntryClosureEntity> _queryEntryEntity_ListClosures(Long entryEntityId) {
        synchronized (this) {
            if (entryEntity_ListClosuresQuery == null) {
                QueryBuilder<EntryClosureEntity> queryBuilder = queryBuilder();
                queryBuilder.join(JoinEntryEntityWithEntryClosureEntity.class, JoinEntryEntityWithEntryClosureEntityDao.Properties.EntryClosureEntityId)
                    .where(JoinEntryEntityWithEntryClosureEntityDao.Properties.EntryEntityId.eq(entryEntityId));
                entryEntity_ListClosuresQuery = queryBuilder.build();
            }
        }
        Query<EntryClosureEntity> query = entryEntity_ListClosuresQuery.forCurrentThread();
        query.setParameter(0, entryEntityId);
        return query.list();
    }

}
